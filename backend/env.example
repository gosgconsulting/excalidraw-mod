# Server Configuration
PORT=4000
NODE_ENV=development

# PostgreSQL Configuration
# Option 1: Use connection string (recommended, takes precedence if set)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/excalidraw

# Option 2: Use individual connection parameters (fallback if DATABASE_URL not set)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=excalidraw
DB_USER=postgres
DB_PASSWORD=postgres

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000,http://localhost:5001

